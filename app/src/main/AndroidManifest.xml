<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practica8bluetoothinternacionalizacion.app" >

    <uses-sdk android:minSdkVersion="11" android:maxSdkVersion="19" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.practica8bluetoothinternacionalizacion.app.Principal"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.practica8bluetoothinternacionalizacion.app.DeviceListActivity"
            android:label="@string/title_activity_device_list"
            android:theme="@android:style/Theme.Holo.Dialog" />
    </application>

</manifest>
